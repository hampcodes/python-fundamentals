# Diccionario = Objeto (JSON)
# NO están ordenados

dictionary = {
    "key": "value",
    "a": 1,
    "b": 2
}

# Diccionario que representa a un usuario
user = {
    "name": "Henry",
    "age": 45,
    "country": "Perú"
}

# Acceder a un valor usando la clave
print(user["age"])      # 45s
print(user["name"])     # Henry
print(user["country"]) # Perú